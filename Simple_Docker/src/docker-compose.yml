version: '3'
  
services:
  app:
    build: .
      
  nginx-proxy:
    image: nginx:latest
    ports:
      - "80:8080"
    volumes:
      - ./nginx/proxy.conf:/etc/nginx/nginx.conf
    depends_on:
      - app